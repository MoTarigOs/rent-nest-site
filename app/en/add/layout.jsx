export const metadata = {
    title: "Advertise a car or property on Rent Nest",
    description: "Add an ad for a car or property on the Rent Nest platform"
};

const Layout = ({ children }) => {
  return (
    
    <main>{children}</main>

  )
};

export default Layout;